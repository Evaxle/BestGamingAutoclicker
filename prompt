go through the gui and make the sections more spaced out for the profile section and mode selection ect and also make the profile handling better so if you save settings in a profile it saves it to that profile and if you load another profile it loads the params and the settings from that profile and make a good enable disable button at the bottom and make the idle and alerts and put it at the bottom make it bigger make it a purple color scheme and make sure when you swap profiles it loads those params and automatically disables the script and when the user starts editing the params it automatically disables the script and when the user tries to start the script with unsaved settings the script will detect and give the user a confirm box to optionally save the settings from that box and other quality of life features and better gui functions and data saving and make sure the autoclicker actually works